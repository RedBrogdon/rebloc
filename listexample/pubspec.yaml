name: listexample
description: An example showing how rebloc can minimize list rebuilds.

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.15.8
  rebloc:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.7.0
  test: ^1.6.3

environment:
  sdk: '>=2.4.0 <3.0.0'

flutter:
  uses-material-design: true
