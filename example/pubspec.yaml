name: example
description: An example showing how the rebloc library works.

dependencies:
  flutter:
    sdk: flutter
  rebloc:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.8.0+1
  test: ^1.9.4

environment:
  sdk: '>=2.4.0 <3.0.0'

flutter:
  uses-material-design: true
